@if(Request.IsAuthenticated) {
   @Html.ActionLink(Context.User.Identity.Name, "Index", "Account")  @:[ @Html.ActionLink("x", "LogOff", "Account") ]
}
else
{
    @:[ @Html.ActionLink("Log On", "LogOn", "Account") ]
}
